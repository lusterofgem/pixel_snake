// Defines the types of blocking input animation (when the animation is running, game will not react to any tap)
enum AnimationType {
  // No animation running
  none,
  // Starting animation
  starting,
  // Pausing animation
  pausing,
  // Unpausing animation
  unpausing,
  // Game over animation
  gameOver,
}
